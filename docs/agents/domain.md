# Domain documentation

This is a single-context repository.

Before implementation, read the root `CONTEXT.md` when present and relevant
records under `docs/adr/`. Use glossary terms consistently in PRDs, issues,
tests, and code. Surface conflicts with an ADR rather than silently overriding
it. Add or revise domain language only when a product decision establishes it.
